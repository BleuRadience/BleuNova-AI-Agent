version: '3'  # Fixed: was "textversion"
services:
  bleunova:
    build: .
    ports:
      - "8000:8000"
      - "${DASHBOARD_PORT:-8501}:8501"
    volumes:
      - .:/app
    env_file:
      - .env
    working_dir: /app
    environment:
      - PYTHONPATH=/app/src
