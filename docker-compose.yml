version: "3"
services:
  bleunova:
    build: .
    ports:
      - "127.0.0.1:8000:8000"
      - "127.0.0.1:8501:8501"
    volumes:
      - ./data:/data
    env_file:
      - .env
    working_dir: /app
    environment:
      - PYTHONPATH=/app/src
